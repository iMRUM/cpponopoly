#include "../../include/controller/PropertyController.hpp"

namespace monopoly {
    // Currently empty - will be implemented as needed
    PropertyController::PropertyController(GameModel* m, GameView* v) : model(m), view(v) {}
}
